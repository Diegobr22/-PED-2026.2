#include <stdio.h>


void mostra(int n[],int tamanho){
    if (n[0] > n[1]){
        printf("O x %d", n[0]);
    }
    else if (n[1] > n[0]) {
        printf("O y %d", n[1]);
    }
    else{
        printf("A");
    }
}

int main(){
    int numeros[2] = {6,4}; 
    mostra(numeros, 2);
}
